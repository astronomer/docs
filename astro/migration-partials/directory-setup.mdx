1. Using the same name as the chosen environment, create a new directory for your project
2. Initialize a new Astro Project in this directory with `astro dev init`
3. Initialize a new git repository in this directory with `git init`, using the git CLI and/or your Source Control Tool’s UI

You can view more detailed instructions and information at the [Create Project](create-project.md#step-1-create-an-astro-project) documentation.